# n, k = [int(x) for x in raw_input().strip().split(' ')]

def add_error(A, B):
    s_a = str(A)
    s_b = str(B)
    ans = 0
    for aa in range(len(s_a)-1, -1, -1):
        

    return ans



T = int(raw_input())

for _ in range(T):
    A = int(raw_input())
    B = int(raw_input())

    actual_sum = A + B
    bob_sum = add_error(A,B)

    # print actual_sum